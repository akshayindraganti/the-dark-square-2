{
  "name": "the-dark-square",
  "version": "1.0.0",
  "description": "The Dark Square — artisan chocolates. Static multi-page site.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && python3 -m http.server 8080",
    "serve": "python3 -m http.server 8080"
  }
}
